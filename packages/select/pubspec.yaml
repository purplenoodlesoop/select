publish_to: none

name: select
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'


dependencies:
  build: ^2.3.0
  source_gen: ^1.2.2
  code_builder: ^4.1.0
  meta: ^1.7.0
  analyzer: ^4.0.0
  select_annotation:
    path:
      ../select_annotation


dev_dependencies:
  purple_lints: ^0.2.0
  test: ^1.16.0
