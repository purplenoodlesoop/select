publish_to: none

name: example
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'


dependencies:
  freezed_annotation: ^1.1.0
  select_annotation:
    path:
      ../select_annotation
#   path: ^1.8.0

dev_dependencies:
  build_runner: ^2.1.10
  freezed: ^1.1.1
  select:
    path:
      ../select
